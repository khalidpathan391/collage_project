import 'package:flutter/material.dart';

class Electri extends StatefulWidget {
  const Electri({Key? key}) : super(key: key);

  @override
  _ElectriState createState() => _ElectriState();
}

class _ElectriState extends State<Electri> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.cyan,
      body: Container(
        child: ListView(
          children: [],
        ),
      ),
    );
  }
}
