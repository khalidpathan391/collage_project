import 'package:flutter/material.dart';

class Rep_Main extends StatefulWidget {
  const Rep_Main({Key? key}) : super(key: key);

  @override
  _Rep_MainState createState() => _Rep_MainState();
}

class _Rep_MainState extends State<Rep_Main> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: ListView(
          children: [],
        ),
      ),
    );
  }
}
